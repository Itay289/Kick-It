require 'spec_helper'

describe TopicsController do
  context "user not sign in" do
    context "user not in DB" do
      
    end

    context "user already in DB" do
      
    end
  end

  context "user signed_in" do
    
  end

end